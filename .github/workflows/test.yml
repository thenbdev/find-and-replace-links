name: Test web link replacement
on: 
  push:
    branches: [develop]
jobs:
  test_web_link_replacement:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout customisations
        uses: actions/checkout@v3
      
      - name: Change to test
        run: cd test

      - name: Check which repo is the workflow currently in
        run: |
          pwd
          ls

      - id: t1
        uses: thenbdev/find-and-replace-links@main
        with: 
          find: example.com
          replace: example1.com
      - id: t2
        uses: thenbdev/find-and-replace-links@main
        with:
          find: www.example.com
          replace: www.example.com

      - id: t3
        uses: thenbdev/find-and-replace-links@main
        with:
          find: www.example.com
          replace: www.example.com

      # Todo: add structure to test and fail the test
